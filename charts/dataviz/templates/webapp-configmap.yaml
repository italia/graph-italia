{{- if .Values.webapp.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dataviz.fullname" . }}-webapp-config
  labels:
    {{- include "dataviz.labels" . | nindent 4 }}
    component: webapp
data:
  config.json: |
    {
      "VITE_SERVER_URL": {{ if .Values.webapp.config.serverUrl }}{{ .Values.webapp.config.serverUrl | quote }}{{ else }}"http://localhost:3003"{{ end }}
    }
{{- end }}


