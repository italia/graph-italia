{{- if .Values.webapp.enabled }}
{{- if .Values.webapp.nginxConfig }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dataviz.fullname" . }}-nginx-config
  labels:
    {{- include "dataviz.labels" . | nindent 4 }}
    component: webapp
data:
  default.conf: |
{{ .Values.webapp.nginxConfig | indent 4 }}
{{- end }}
{{- end }}

