VITE_SERVER_URL="http://127.0.0.1:3003"

# Public chart/dashboard publishing is NOT configured here: it is a server
# setting (ENABLE_PUBLIC_PUBLISHING in packages/server/.env) that the webapp
# reads at startup from GET /api/config, so API and UI can never disagree.
